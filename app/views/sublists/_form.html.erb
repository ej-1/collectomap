<h2>Create new sublist</h2>

<%= form_for @sublist, :remote => true do |f| %>
  <% if @sublist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sublist.errors.count, "error") %> prohibited this sublist from being saved:</h2>

      <ul>
      <% @sublist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
    <%= f.hidden_field :list_id, :value => @list.id %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
