<%= form_for(@sublist_item) do |f| %>
  <% if @sublist_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sublist_item.errors.count, "error") %> prohibited this sublist_item from being saved:</h2>

      <ul>
      <% @sublist_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :adress %><br>
    <%= f.text_field :adress %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
